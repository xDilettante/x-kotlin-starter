<configuration>
    <property name="USER_HOME" value='/home/user/'/>

    <appender name="LOG" class="ch.qos.logback.core.FileAppender">
        <!--append to file-->
        <file>
            ${USER_HOME}/myApp.log
        </file>
        <encoder>
            <pattern>[ %d{yyyy-MM-dd HH:mm:ss.SSS z} ] [ %-5level ] [%thread] %logger{36}.%file:%line\t[ %M ]\n\t%msg\n\n</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <!-- encoders are assigned the type
             ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
        <encoder>
            <pattern>[ %d{yyyy-MM-dd HH:mm:ss.SSS z} ] [ %-5level ] [%thread] %logger{36}.%file:%line\t[ %M ]\n\t%msg\n\n</pattern>
        </encoder>
    </appender>

    <logger name="x.metabit" level="TRACE"/>
    <logger name="x.metaint" level="TRACE"/>
    <logger name="x.metasoft" level="TRACE"/>

    <logger name="ch.qos.logback" level="WARN"/>



    <root level="WARN">
        <appender-ref ref="LOG"/>
        <appender-ref ref="STDOUT" />
    </root>
</configuration>